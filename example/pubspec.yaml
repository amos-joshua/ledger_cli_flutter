name: example
description: Example for ledger_cli_flutter
version: 0.1.0
publish_to: 'none'

environment:
  sdk: '>=2.18.6 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  file_picker: 5.2.5

  ledger_cli: ^0.1.1
    #path: ../../ledger-cli-dart
  ledger_cli_flutter:
    path: ../

  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
